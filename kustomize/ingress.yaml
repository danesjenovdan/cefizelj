apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: dz-2022-add-trailing-slash
spec:
  redirectRegex:
    regex: (/volitve/dz-2022)(\?.*)?$
    replacement: ${1}/${2}
    permanent: false
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: dz-2022-strip-prefix
spec:
  stripPrefix:
    prefixes:
      - /volitve/dz-2022
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: volitveni-vodic
  annotations:
    # Do not put cert manager here, root redirect ingress (in `ops` repo) will handle it.
    # If we put it here, they will both try to issue the certificate and letsencrypt will rate limit us.
    traefik.ingress.kubernetes.io/router.middlewares: |
      traefik-redirect-to-https@kubernetescrd,
      vodici-redirect-domain@kubernetescrd,
      vodici-dz-2022-add-trailing-slash@kubernetescrd,
      vodici-dz-2022-strip-prefix@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - host: vodici.danesjenovdan.si
      http:
        paths:
          - path: /volitve
            pathType: Prefix
            backend:
              service:
                name: volitveni-vodic
                port:
                  number: 80
