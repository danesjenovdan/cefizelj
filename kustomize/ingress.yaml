apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: evropske-add-trailing-slash
spec:
  redirectRegex:
    regex: (/volitve/evropske)(\?.*)?$
    replacement: ${1}/${2}
    permanent: false
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: evropske-strip-prefix
spec:
  stripPrefix:
    prefixes:
      - /volitve/evropske
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: volitveni-vodic-evropske-2024
  annotations:
    # Do not put cert manager here, root redirect ingress (in `ops` repo) will handle it.
    # If we put it here, they will both try to issue the certificate and letsencrypt will rate limit us.
    traefik.ingress.kubernetes.io/router.middlewares: |
      traefik-redirect-to-https@kubernetescrd,
      vodici-redirect-domain@kubernetescrd,
      vodici-evropske-add-trailing-slash@kubernetescrd,
      vodici-evropske-strip-prefix@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - host: vodici.danesjenovdan.si
      http:
        paths:
          - path: /volitve/evropske
            pathType: Prefix
            backend:
              service:
                name: volitveni-vodic-evropske-2024
                port:
                  number: 80
