apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: za-add-trailing-slash
spec:
  redirectRegex:
    regex: (/referendum/za)$
    replacement: ${1}/
    permanent: false
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: za-strip-prefix
spec:
  stripPrefix:
    prefixes:
      - /referendum/za
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: volitveni-vodic-za
  annotations:
    # Do not put cert manager here, root redirect ingress (in `ops` repo) will handle it.
    # If we put it here, they will both try to issue the certificate and letsencrypt will rate limit us.
    traefik.ingress.kubernetes.io/router.middlewares: |
      traefik-redirect-to-https@kubernetescrd,
      vodici-redirect-domain@kubernetescrd,
      vodici-za-add-trailing-slash@kubernetescrd,
      vodici-za-strip-prefix@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - host: vodici.danesjenovdan.si
      http:
        paths:
          - path: /referendum/za
            pathType: Prefix
            backend:
              service:
                name: volitveni-vodic-za
                port:
                  number: 80
