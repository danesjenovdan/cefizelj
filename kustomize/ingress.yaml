apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: razkroj-2026-add-trailing-slash
spec:
  redirectRegex:
    regex: (/referendum/razkroj)(\?.*)?$
    replacement: ${1}/${2}
    permanent: false
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: razkroj-2026-strip-prefix
spec:
  stripPrefix:
    prefixes:
      - /referendum/razkroj
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: volitveni-vodic-razkroj-2026
  annotations:
    # Do not put cert manager here, root redirect ingress (in `ops` repo) will handle it.
    # If we put it here, they will both try to issue the certificate and letsencrypt will rate limit us.
    traefik.ingress.kubernetes.io/router.middlewares: |
      traefik-redirect-to-https@kubernetescrd,
      vodici-redirect-domain@kubernetescrd,
      vodici-razkroj-2026-add-trailing-slash@kubernetescrd,
      vodici-razkroj-2026-strip-prefix@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - host: vodici.danesjenovdan.si
      http:
        paths:
          - path: /referendum/razkroj
            pathType: Prefix
            backend:
              service:
                name: volitveni-vodic-razkroj-2026
                port:
                  number: 80
