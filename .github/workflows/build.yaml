name: Build and publish Docker image
on:
  push:
    branches:
      - "mocljudem-digitalna-identiteta-2026"
jobs:
  push_to_registry:
    name: Build and push Docker image to registry
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Scaleway
        uses: docker/login-action@v4
        with:
          registry: rg.fr-par.scw.cloud/djnd
          username: nologin
          password: ${{ secrets.SCW_SECRET_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v7
        env:
          DOCKER_BUILD_SUMMARY: false
          DOCKER_BUILD_RECORD_UPLOAD: false
        with:
          context: .
          build-args: |
            COMMIT_SHA=${{ github.sha }}
          push: true
          tags: |
            rg.fr-par.scw.cloud/djnd/volitveni-vodic-mocljudem-digitalna-identiteta-2026:${{ github.sha }}
            rg.fr-par.scw.cloud/djnd/volitveni-vodic-mocljudem-digitalna-identiteta-2026:latest

      - name: Update image versions in kustomize/kustomization.yaml
        run: |
          sed -i "s/newTag: .*\+/newTag\: '${{ github.sha }}'/" kustomize/kustomization.yaml

      - name: Commit and push new image versions
        uses: EndBug/add-and-commit@v10
        with:
          add: "."
          message: "Update image versions in kustomize/kustomization.yaml"
